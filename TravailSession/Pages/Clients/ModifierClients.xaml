<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailSession.Pages.Clients.ModifierClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailSession.Pages.Clients"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button x:Name="btPecedent" Click="btPecedent_Click" Grid.Row="0">Pécédent</Button>
        <Grid x:Name="gridRacine"/>
        <StackPanel Grid.Row="1" Spacing="10" Padding="0 20 0 0">
            <TextBlock x:Name="tblTitre" Text="Créer un Client" FontSize="30" FontWeight="Bold" FontFamily="Times New Roman"/>
            <TextBox x:Name="tbxNom" FontSize="18" FontWeight="Bold" Header="Nom: "/>
            <TextBox x:Name="tbxAdresse" Header="Adresse: "/>
            <TextBox x:Name="tbxNumeroTelephone" Header="Numéro de téléphone :"/>
            <TextBox x:Name="tbxEmail" Header="Email: "/>
            <Button x:Name="btnModifier" Content="Modifier" Click="btnModifier_Click"/>
            <Button x:Name="btnSupprimer" Content="Supprimer" Click="btnSupprimer_Click"/>
        </StackPanel>
    </Grid>
</Page>
