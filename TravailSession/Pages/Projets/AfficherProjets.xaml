<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailSession.Pages.Projets.AfficherProjets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailSession.Pages.Projets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:TravailSession.Class"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock x:Name="tblTexte" Text="Liste des Projets" FontSize="30" FontWeight="Bold" FontFamily="Times New Roman"/>
        </StackPanel>

        <GridView x:Name="lvListeProjects" Grid.Row="1" SelectionChanged="lvListeProjects_SelectionChanged">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Projet">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{x:Bind NumeroProjet}" FontSize="18" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Titre}" FontSize="14"/>
                            <TextBlock Text="{x:Bind GetClientName()}" FontSize="14"/>
                            <TextBlock Text="{x:Bind DateDebut}" FontSize="14"/>
                            <TextBlock Text="{x:Bind Budget}" FontSize="14"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
