<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailSession.Pages.Projets.ZoomProjets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailSession.Pages.Projets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button x:Name="btPecedent" Click="btPecedent_Click" Grid.Row="0">Pécédent</Button>
        <Grid x:Name="gridRacine"/>
        <StackPanel Grid.Row="1" Spacing="10" Padding="0 20 0 0">
            
            <StackPanel>
                    <TextBlock x:Name="tblNumeroProjet" FontSize="30" FontWeight="Bold" FontFamily="Times New Roman"/>
                    <TextBlock x:Name="tblTitre" FontSize="18" FontWeight="Bold"/>
                </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <StackPanel>
                    <TextBlock x:Name="tblStatutFix" Text="Statut : "/>
                    <TextBlock x:Name="tblDateDebutFix" Text="Date de debut: "/>
                    <TextBlock x:Name="tblBudgetFix" Text="Budget : "/>
                    <TextBlock x:Name="tblTotalSalairesFix" Text="Total salaires : "/>
                    <TextBlock x:Name="tblNombreEmployesRequisFix" Text="Nombre d'employes requis: "/>
                    <TextBlock x:Name="tblIdentifantFix" Text="Identifant clients: "/>
                    <TextBlock x:Name="tblClientsNameFix" Text="Noms clients: "/>
                </StackPanel>

                <StackPanel>
                    <TextBlock x:Name="tblStatut"/>
                    <TextBlock x:Name="tblDateDebut"/>
                    <TextBlock x:Name="tblBudget"/>
                    <TextBlock x:Name="tblTotalSalaires"/>
                    <TextBlock x:Name="tblNombreEmployesRequis"/>
                    <TextBlock x:Name="tblIdentifant"/>
                    <TextBlock x:Name="tblClientsName"/>
                </StackPanel>

                <TextBlock x:Name="tblDescription"/>
            </StackPanel>
            <ListView x:Name="lvEmployes">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <TextBlock Text="{Binding Matricule}" Width="120"/>
                            <TextBlock Text="{Binding Nom}" Width="150"/>
                            <TextBlock Text="{Binding Prenom}" Width="150"/>
                            <TextBlock Text="{Binding TauxHoraire}" Width="100"/>
                            <TextBlock Text="{Binding Heures}" Width="80"/>
                            <TextBlock Text="{Binding Salaire}" Width="120"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBox x:Name="tbxMatricule" Header="Matricule de l'employes"/>
            <NumberBox x:Name="nbxHeure" Header="Le nombre d'heure de travailer par l'employe"/>
            <Button x:Name="btAjouterEmploye" Click="btAjouterEmploye_Click" Grid.Row="2">Ajouter Employe</Button>
        </StackPanel>

        <Button x:Name="btModifier" Click="btModifier_Click" Grid.Row="2">Modifier</Button>
        
    </Grid>
</Page>
