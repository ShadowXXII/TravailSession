<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TravailSession.Pages.Employes.ModifierEmployes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravailSession.Pages.Employes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
        <Button x:Name="btPecedent" Click="btPecedent_Click" >Pécédent</Button>
        <Button x:Name="btnModifier" Content="Modifier" Click="btnModifier_Click"/>
        <Button x:Name="btnSupprimer" Content="Supprimer" Click="btnSupprimer_Click"/>
        </StackPanel>
        <Grid x:Name="gridRacine"/>
        <StackPanel Grid.Row="1" Padding="0 20 0 0">
            <TextBlock x:Name="tblTitre" FontSize="30" FontWeight="Bold" FontFamily="Times New Roman"/>
            <TextBox x:Name="tbxNom" FontSize="18" FontWeight="Bold" Header="Nom:"/>
            <TextBlock x:Name="tblErreurNom"/>
            <TextBox x:Name="tbxPrenom" FontSize="18" FontWeight="Bold" Header="Prenom:"/>
            <TextBlock x:Name="tblErreurPrenom"/>
            <TextBox x:Name="tbxEmail" Header="Email"/>
            <TextBlock x:Name="tblErreurEmail"/>
            <TextBox x:Name="tbxPhotoIdentite" Header="Lien pour l'image de profile"/>
            <TextBlock x:Name="tblErreurPhotoIdentite"/>
            <NumberBox x:Name="nbxTauxHoraire" Header="Taux horaire :"/>
            <TextBlock x:Name="tblErreurHoraire"/>
        </StackPanel>
    </Grid>
</Page>
